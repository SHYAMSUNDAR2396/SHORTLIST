# Candidate Ranking System - test/dev dependencies.
# CPU-only, no network/LLM/GPU dependencies. The ranking pipeline itself uses
# only the Python standard library; these packages support testing only.

hypothesis==6.155.2
pytest==8.3.5
pytest-timeout==2.4.0
